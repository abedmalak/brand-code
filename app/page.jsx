const Home = () => (
  <section className='w-full flex-center flex-col'> {/**
   * the components have full width, the are centered, and they stack on top of each other
   */}
  </section>
);

export default Home;
